# dependencies
node_modules/

# build output
dist/
build/

# local data store (recreated and reseeded automatically)
.data/

# testing
coverage/
playwright-report/
test-results/
# the e2e suite's scratch store — DPAS_DATA_DIR in playwright.config.ts, so a
# suite that issues invoices never touches the one above
.data-e2e/

# env — .env.example is the committed reference
.env
.env.local
.env.*.local

# misc
.DS_Store
*.tsbuildinfo
